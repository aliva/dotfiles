#!/bin/bash

export DISPLAY=:0.0

function run(){
    echo $1
    if pgrep $1 &> /dev/null
    then
        echo $1 is already running
    else
        $1 &> /dev/null &
    fi
}

run thunderbird
run firefox
run pidgin
run rhythmbox
