#!/bin/bash

export DISPLAY=:0.0
export LANG=fa_IR.utf8
export LC_ALL=$LANG

function run(){
    echo $1
    if pgrep $1 &> /dev/null
    then
        echo $1 is already running
    else
        $1 &> /dev/null &
    fi
}

run thunderbird
run firefox
run pidgin
run rhythmbox
