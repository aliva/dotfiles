[user]
    email = ali@vakilzade.ir
    name = Ali Vakilzade
[alias]
    st = status

    # simply cd to root of project
    # cd `git root`
    root = rev-parse --show-toplevel
[core]
    excludesfile = ~/Workspace/dotfiles/gitignore
[push]
    default = simple

# vim:set ft=cfg:

