# yep, glob is simple, why should I use regex while glob is good enough for me?
syntax:glob

# ignore komodo project files, It's just silly to add them to repo
*.komodoproject

# ignore python stupid files
__pycache__
*.pyc
